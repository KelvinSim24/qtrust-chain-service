export declare class Pagination {
    readonly totalEntries: number;
    readonly pageNumber: number;
    readonly pageSize: number;
    readonly totalPages: number;
    /**
     * @param totalEntries
     * @param pageNumber
     * @param pageSize
     * @param totalPages
     */
    constructor(totalEntries: number, pageNumber: number, pageSize: number, totalPages: number);
}
