import { DownloadChannel } from './DownloadChannel';
import { Pagination } from '../Pagination';
export declare class DownloadChannelSearch {
    downloadChannelsInfo: DownloadChannel[];
    readonly pagination: Pagination;
    constructor(downloadChannelsInfo: DownloadChannel[], pagination: Pagination);
}
