import { Address } from '../account/Address';
import { UInt64 } from '../UInt64';
import { DriveInfoDTO } from '../../infrastructure/model/storage/driveInfoDTO';
export type UsedSize = {
    replicator: string;
    size: UInt64;
};
export type UsedSizeDTO = {
    replicator: string;
    size: number[];
};
export type DataModification = {
    id: string;
    owner: string;
    downloadDataCdi: string;
    expectedUploadSize: UInt64;
    actualUploadSize: UInt64;
    folderName: string;
    readyForApproval: boolean;
    isStream: boolean;
    state: number;
    success: number;
};
export type DataModificationDTO = {
    id: string;
    owner: string;
    downloadDataCdi: string;
    expectedUploadSize: number[];
    actualUploadSize: number[];
    folderName: string;
    readyForApproval: boolean;
    isStream: boolean;
    state: number;
    success: number;
};
export type ShardReplicator = {
    key: string;
    uploadSize: UInt64;
};
export type DataModificationShard = {
    replicator: string;
    actualShardReplicators: ShardReplicator[];
    formerShardReplicators: ShardReplicator[];
    ownerUpload: UInt64;
};
export type ShardReplicatorDTO = {
    key: string;
    uploadSize: number[];
};
export type DataModificationShardDTO = {
    replicator: string;
    actualShardReplicators: ShardReplicatorDTO[];
    formerShardReplicators: ShardReplicatorDTO[];
    ownerUpload: number[];
};
export declare class DriveInfo {
    readonly multisig: string;
    readonly multisigAddress: Address;
    readonly owner: string;
    readonly rootHash: string;
    readonly lastModificationId: string;
    readonly size: UInt64;
    readonly usedSizeBytes: UInt64;
    readonly metaFilesSizeBytes: UInt64;
    readonly replicatorCount: number;
    readonly ownerManagement: number;
    readonly activeDataModifications: DataModification[];
    readonly completedDataModifications: DataModification[];
    readonly confirmedUsedSizes: UsedSize[];
    readonly replicators: string[];
    readonly offboardingReplicators: string[];
    readonly downloadShards: string[];
    readonly dataModificationShards: DataModificationShard[];
    readonly metaId?: string | undefined;
    constructor(multisig: string, multisigAddress: Address, owner: string, rootHash: string, lastModificationId: string, size: UInt64, usedSizeBytes: UInt64, metaFilesSizeBytes: UInt64, replicatorCount: number, ownerManagement: number, activeDataModifications: DataModification[], // need verify
    completedDataModifications: DataModification[], confirmedUsedSizes: UsedSize[], replicators: string[], offboardingReplicators: string[], // need verify
    downloadShards: string[], // need verify
    dataModificationShards: DataModificationShard[], metaId?: string | undefined);
    static createFromDriveDTO(driveDTO: DriveInfoDTO, metaId?: string): DriveInfo;
    static createFromDTO(multisig: string, multisigAddress: string, owner: string, rootHash: string, lastModificationId: string, size: number[], usedSizeBytes: number[], metaFilesSizeBytes: number[], replicatorCount: number, ownerManagement: number, activeDataModifications: DataModificationDTO[], // need verify
    completedDataModifications: DataModificationDTO[], confirmedUsedSizes: UsedSizeDTO[], replicators: string[], offboardingReplicators: string[], // need verify
    downloadShards: string[], // need verify
    dataModificationShards: DataModificationShardDTO[], metaId?: string): DriveInfo;
}
