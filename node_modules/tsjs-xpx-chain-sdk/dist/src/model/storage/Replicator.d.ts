import { UInt64 } from '../UInt64';
import { ReplicatorDTO } from '../../infrastructure/model/storage/replicatorDTO';
export type DriveWork = {
    drive: string;
    lastApprovedDataModificationId: string;
    initialDownloadWork: UInt64;
    lastCompletedCumulativeDownloadWork: UInt64;
};
export type DriveWorkDTO = {
    drive: string;
    lastApprovedDataModificationId: string;
    initialDownloadWork: number[];
    lastCompletedCumulativeDownloadWork: number[];
};
export declare class Replicator {
    readonly key: string;
    readonly version: number;
    readonly drives: DriveWork[];
    readonly downloadChannels: string[];
    readonly metaId?: string | undefined;
    constructor(key: string, version: number, drives: DriveWork[], downloadChannels: string[], // need verify
    metaId?: string | undefined);
    static createFromReplicatorDTO(dto: ReplicatorDTO, metaId?: string): Replicator;
    static createFromDTO(key: string, version: number, drives: DriveWorkDTO[], downloadChannels: string[], // need verify
    metaId?: string): Replicator;
}
