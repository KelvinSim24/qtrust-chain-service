export declare class BlockchainVersion {
    readonly build: number;
    readonly revision: number;
    readonly minor: number;
    readonly major: number;
    constructor(build: number, revision: number, minor: number, major: number);
}
