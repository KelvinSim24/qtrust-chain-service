import { DerivationScheme } from '../../core/crypto';
import { PublicAccount } from '../account/PublicAccount';
import { Message } from './Message';
import { PlainMessage } from './PlainMessage';
/**
 * Encrypted Message model
 */
export declare class EncryptedMessage extends Message {
    constructor(payload: string);
    /**
     *
     * @param message - Plain message to be encrypted
     * @param recipientPublicAccount - Recipient public account
     * @param privateKey - Sender private key
     * @param {DerivationScheme} dScheme The derivation scheme
     * @return {EncryptedMessage}
     */
    static create(message: string, recipientPublicAccount: PublicAccount, privateKey: string, dScheme?: DerivationScheme): EncryptedMessage;
    /**
     *
     * @param payload
     */
    static createFromPayload(payload: string): EncryptedMessage;
    /**
     *
     * @param encryptMessage - Encrypted message to be decrypted
     * @param privateKey - Recipient private key
     * @param recipientPublicAccount - Sender public account
     * @param {DerivationScheme} dScheme The derivation scheme
     * @return {PlainMessage}
     */
    static decrypt(encryptMessage: EncryptedMessage, privateKey: string, recipientPublicAccount: PublicAccount, dScheme?: DerivationScheme): PlainMessage;
}
