import { PublicAccount } from '../../account/PublicAccount';
import { NetworkType } from '../../blockchain/NetworkType';
import { UInt64 } from '../../UInt64';
import { Deadline } from '../Deadline';
import { Transaction, TransactionBuilder } from '../Transaction';
import { TransactionInfo } from '../TransactionInfo';
export declare class NewDownloadTransaction extends Transaction {
    readonly driveKey: PublicAccount;
    readonly downloadSize: UInt64;
    readonly feedbackFeeAmount: UInt64;
    readonly listOfPublicKeys: PublicAccount[];
    /**
     * Create a new replicator onboarding transaction object
     * @param deadline - The deadline to include the transaction.
     * @param driveKey - Public key of the drive
     * @param downloadSize - Prepaid Download Size in Mb
     * @param feedbackFeeAmount - XPXs to lock for future payment for
     * @param listOfPublicKeys - List of public keys
     * @param networkType - The network type.
     * @param maxFee - (Optional) Max fee defined by the sender
     * @returns {NewDownloadTransaction}
     */
    static create(deadline: Deadline, driveKey: PublicAccount, downloadSize: UInt64, feedbackFeeAmount: UInt64, listOfPublicKeys: PublicAccount[], networkType: NetworkType, maxFee?: UInt64): NewDownloadTransaction;
    /**
     * @param networkType
     * @param version
     * @param deadline
     * @param maxFee
     * @param driveKey - Public key of the drive
     * @param downloadSize - Prepaid Download Size in Mb
     * @param feedbackFeeAmount - XPXs to lock for future payment for
     * @param listOfPublicKeys - List of public keys
     * @param signature
     * @param signer
     * @param transactionInfo
     */
    constructor(networkType: NetworkType, version: number, deadline: Deadline, maxFee: UInt64, driveKey: PublicAccount, downloadSize: UInt64, feedbackFeeAmount: UInt64, listOfPublicKeys: PublicAccount[], signature?: string, signer?: PublicAccount, transactionInfo?: TransactionInfo);
    /**
     * @override Transaction.size()
     * @description get the byte size of a NewDownloadTransaction
     * @returns {number}
     * @memberof NewDownloadTransaction
     */
    get size(): number;
    static calculateSize(totalPublicKeys: number): number;
    /**
     * @override Transaction.toJSON()
     * @description Serialize a transaction object - add own fields to the result of Transaction.toJSON()
     * @return {Object}
     * @memberof NewDownloadTransaction
     */
    toJSON(): {
        transaction: {
            driveKey: string;
            downloadSize: number[];
            feedbackFeeAmount: number[];
            listOfPublicKeys: {
                publicKey: string;
                address: {
                    address: string;
                    networkType: NetworkType;
                };
            }[];
            type: number;
            networkType: NetworkType;
            version: number;
            maxFee: number[];
            deadline: number[];
            signature: string;
        };
    };
}
export declare class NewDownloadTransactionBuilder extends TransactionBuilder {
    private _driveKey;
    private _downloadSize;
    private _feedbackFeeAmount;
    private _listOfPublicKeys;
    driveKey(driveKey: PublicAccount): this;
    downloadSize(downloadSize: UInt64): this;
    feedbackFeeAmount(feedbackFeeAmount: UInt64): this;
    listOfPublicKeys(listOfPublicKeys: PublicAccount[]): this;
    build(): NewDownloadTransaction;
}
