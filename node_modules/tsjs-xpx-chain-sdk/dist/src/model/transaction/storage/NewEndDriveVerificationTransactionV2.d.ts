import { PublicAccount } from '../../account/PublicAccount';
import { NetworkType } from '../../blockchain/NetworkType';
import { UInt64 } from '../../UInt64';
import { Deadline } from '../Deadline';
import { Transaction, TransactionBuilder } from '../Transaction';
import { TransactionInfo } from '../TransactionInfo';
export declare class NewEndDriveVerificationV2Transaction extends Transaction {
    readonly driveKey: PublicAccount;
    readonly verificationTrigger: string;
    readonly shardId: number;
    readonly keys: PublicAccount[];
    readonly signatures: string[];
    readonly opinions: number;
    /**
     * Create a new replicator onboarding transaction object
     * @param deadline - The deadline to include the transaction.
     * @param driveKey - Public key of the drive
     * @param verificationTrigger - The hash of block that initiated the Verification
     * @param shardId - Shard identifier, type unsigned short integer
     * @param keys - Replicators' public keys
     * @param signatures - Signatures of replicators' opinions
     * @param opinions - Two-dimensional bit array of opinions (1 is success, 0 is failure)
     * @param networkType - The network type.
     * @param maxFee - (Optional) Max fee defined by the sender
     * @returns {NewEndDriveVerificationV2Transaction}
     */
    static create(deadline: Deadline, driveKey: PublicAccount, verificationTrigger: string, shardId: number, keys: PublicAccount[], signatures: string[], opinions: number, networkType: NetworkType, maxFee?: UInt64): NewEndDriveVerificationV2Transaction;
    /**
     * @param networkType
     * @param version
     * @param deadline
     * @param maxFee
     * @param driveKey - The drive key
     * @param verificationTrigger - hash (32 bytes)
     * @param shardId - shard id of type short unsigned integer
     * @param keys - punlic keys
     * @param signatures - Signatures array (64 bytes)
     * @param opinions - uint8
     * @param signature
     * @param signer
     * @param transactionInfo
     */
    constructor(networkType: NetworkType, version: number, deadline: Deadline, maxFee: UInt64, driveKey: PublicAccount, verificationTrigger: string, shardId: number, keys: PublicAccount[], signatures: string[], opinions: number, signature?: string, signer?: PublicAccount, transactionInfo?: TransactionInfo);
    /**
     * @override Transaction.size()
     * @description get the byte size of a NewEndDriveVerificationV2Transaction
     * @returns {number}
     * @memberof NewEndDriveVerificationV2Transaction
     */
    get size(): number;
    static calculateSize(keysCount: number, judgingKeyCount: number): number;
    /**
     * @override Transaction.toJSON()
     * @description Serialize a transaction object - add own fields to the result of Transaction.toJSON()
     * @return {Object}
     * @memberof NewEndDriveVerificationV2Transaction
     */
    toJSON(): {
        transaction: {
            driveKey: string;
            verificationTrigger: string;
            shardId: number;
            keys: {
                publicKey: string;
                address: {
                    address: string;
                    networkType: NetworkType;
                };
            }[];
            signatures: string[];
            opinions: number;
            type: number;
            networkType: NetworkType;
            version: number;
            maxFee: number[];
            deadline: number[];
            signature: string;
        };
    };
}
export declare class NewEndDriveVerificationV2TransactionBuilder extends TransactionBuilder {
    private _driveKey;
    private _verificationTrigger;
    private _shardId;
    private _keys;
    private _signatures;
    private _opinions;
    driveKey(driveKey: PublicAccount): this;
    verificationTrigger(verificationTrigger: string): this;
    shardId(shardId: number): this;
    keys(keys: PublicAccount[]): this;
    signatures(signatures: string[]): this;
    opinions(opinions: number): this;
    build(): NewEndDriveVerificationV2Transaction;
}
