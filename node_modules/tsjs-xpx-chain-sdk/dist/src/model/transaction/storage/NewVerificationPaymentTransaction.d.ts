import { PublicAccount } from '../../account/PublicAccount';
import { NetworkType } from '../../blockchain/NetworkType';
import { UInt64 } from '../../UInt64';
import { Deadline } from '../Deadline';
import { Transaction, TransactionBuilder } from '../Transaction';
import { TransactionInfo } from '../TransactionInfo';
export declare class NewVerificationPaymentTransaction extends Transaction {
    readonly driveKey: PublicAccount;
    readonly verificationFeeAmount: UInt64;
    /**
     * Create a new replicator onboarding transaction object
     * @param deadline - The deadline to include the transaction.
     * @param driveKey - Public key of the drive
     * @param verificationFeeAmount - Amount of XPXs to transfer to the drive
     * @param networkType - The network type.
     * @param maxFee - (Optional) Max fee defined by the sender
     * @returns {NewVerificationPaymentTransaction}
     */
    static create(deadline: Deadline, driveKey: PublicAccount, verificationFeeAmount: UInt64, networkType: NetworkType, maxFee?: UInt64): NewVerificationPaymentTransaction;
    /**
     * @param networkType
     * @param version
     * @param deadline
     * @param maxFee
     * @param driveKey - The drive key
     * @param verificationFeeAmount - The verification fee amount
     * @param signature
     * @param signer
     * @param transactionInfo
     */
    constructor(networkType: NetworkType, version: number, deadline: Deadline, maxFee: UInt64, driveKey: PublicAccount, verificationFeeAmount: UInt64, signature?: string, signer?: PublicAccount, transactionInfo?: TransactionInfo);
    /**
     * @override Transaction.size()
     * @description get the byte size of a NewVerificationPaymentTransaction
     * @returns {number}
     * @memberof NewVerificationPaymentTransaction
     */
    get size(): number;
    static calculateSize(): number;
    /**
     * @override Transaction.toJSON()
     * @description Serialize a transaction object - add own fields to the result of Transaction.toJSON()
     * @return {Object}
     * @memberof NewVerificationPaymentTransaction
     */
    toJSON(): {
        transaction: {
            driveKey: {
                publicKey: string;
                address: {
                    address: string;
                    networkType: NetworkType;
                };
            };
            verificationFeeAmount: number[];
            type: number;
            networkType: NetworkType;
            version: number;
            maxFee: number[];
            deadline: number[];
            signature: string;
        };
    };
}
export declare class NewVerificationPaymentTransactionBuilder extends TransactionBuilder {
    private _driveKey;
    private _verificationFeeAmountSize;
    driveKey(driveKey: PublicAccount): this;
    verificationFeeAmount(verificationFeeAmount: UInt64): this;
    build(): NewVerificationPaymentTransaction;
}
