import { PublicAccount } from '../../account/PublicAccount';
import { NetworkType } from '../../blockchain/NetworkType';
import { UInt64 } from '../../UInt64';
import { Deadline } from '../Deadline';
import { Transaction, TransactionBuilder } from '../Transaction';
import { TransactionInfo } from '../TransactionInfo';
export declare class ReplicatorOffboardingTransaction extends Transaction {
    readonly driveKey: PublicAccount;
    /**
     * Create a new replicator onboarding transaction object
     * @param deadline - The deadline to include the transaction.
     * @param driveKey - Public key of the drive
     * @param networkType - The network type.
     * @param maxFee - (Optional) Max fee defined by the sender
     * @returns {ReplicatorOffboardingTransaction}
     */
    static create(deadline: Deadline, driveKey: PublicAccount, networkType: NetworkType, maxFee?: UInt64): ReplicatorOffboardingTransaction;
    /**
     * @param networkType
     * @param version
     * @param deadline
     * @param maxFee
     * @param driveKey
     * @param signature
     * @param signer
     * @param transactionInfo
     */
    constructor(networkType: NetworkType, version: number, deadline: Deadline, maxFee: UInt64, driveKey: PublicAccount, signature?: string, signer?: PublicAccount, transactionInfo?: TransactionInfo);
    /**
     * @override Transaction.size()
     * @description get the byte size of a ReplicatorOffboardingTransaction
     * @returns {number}
     * @memberof ReplicatorOffboardingTransaction
     */
    get size(): number;
    static calculateSize(): number;
    /**
     * @override Transaction.toJSON()
     * @description Serialize a transaction object - add own fields to the result of Transaction.toJSON()
     * @return {Object}
     * @memberof ReplicatorOffboardingTransaction
     */
    toJSON(): {
        transaction: {
            driveKey: {
                publicKey: string;
                address: {
                    address: string;
                    networkType: NetworkType;
                };
            };
            type: number;
            networkType: NetworkType;
            version: number;
            maxFee: number[];
            deadline: number[];
            signature: string;
        };
    };
}
export declare class ReplicatorOffboardingTransactionBuilder extends TransactionBuilder {
    private _driveKey;
    driveKey(driveKey: PublicAccount): this;
    build(): ReplicatorOffboardingTransaction;
}
