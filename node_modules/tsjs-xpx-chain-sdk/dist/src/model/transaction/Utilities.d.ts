import { UInt64 } from '../UInt64';
import { Deadline } from './Deadline';
import { NetworkType } from '../blockchain/NetworkType';
import { MultisigAccountGraphInfo } from '../account/MultisigAccountGraphInfo';
import { PublicAccount } from '../account/PublicAccount';
import { MosaicId } from '../mosaic/MosaicId';
import { NamespaceId } from '../namespace/NamespaceId';
import { MetadataType } from '../metadata/MetadataType';
export declare enum TimeUnit {
    DAY = 4,
    HOUR = 3,
    MINUTE = 2,
    SECOND = 1,
    MILLISECOND = 0
}
/**
 * @param versionHex - Transaction version in hex
 * @returns {NetworkType}
 */
export declare const extractNetwork: (versionHex: string) => NetworkType;
export declare const hasBit: (numberToCompare: number, bitPosition: number) => boolean;
export declare const hasBits: (numberToCompare: number, bitsNumber: number) => boolean;
export declare const convertStringTimeUnit: (startUnit: string, targetUnit: number) => number;
export declare const convertTimeUnit: (amount: number, startUnit: TimeUnit, targetUnit: TimeUnit) => number;
export declare const checkCosigner: (cosignerPublicKey: string, multisigAccPublicKey: string, multisigAccGraph: MultisigAccountGraphInfo) => {
    isCosigner: boolean;
    cosignerSignable: boolean;
    aggComplete: boolean;
};
/**
 * Get lower value from maxBondedTransactionLifetime and maxHashLockDuration,
 * comparison is done with second.
 * This ensure expiration of both life time and lock duration of Aggregate Bonded Transaction
 * can be set using almost the nearest time.
 * @param maxBondedTransactionLifetime - Network config of max bonded transaction life time
 * @param maxHashLockDuration - Network config of max hash lock duration
 * @returns
 */
export declare const getABTMinConfigSeconds: (maxBondedTransactionLifetime: number, maxHashLockDuration: number) => number;
/**
 * Get a maximum block duration for Aggregate Bonded Transaction for Sirius Chain
 * @param blockGenerationTargetTime - Network config of targeted block generation time
 * @param maxBondedTransactionLifetime - Network config of max bonded transaction life time
 * @param maxHashLockDuration - Network config of max hash lock duration
 * @returns
 */
export declare const getABTMaxSafeDurationWithSeconds: (blockGenerationTargetTime: number, maxBondedTransactionLifetime: number, maxHashLockDuration: number) => number;
/**
 * Get a maximum Deadline for Aggregate Bonded Transaction for Sirius Chain
 * @param maxBondedTransactionLifetime - Network config of max bonded transaction life time
 * @param maxHashLockDuration - Network config of max hash lock duration
 * @param currentNodeTimetamp - When added, it is able to create a Deadline that is Daylight saving time safe.
 * @returns
 */
export declare const getABTMaxSafeDeadlineWithSeconds: (maxBondedTransactionLifetime: number, maxHashLockDuration: number, currentNodeTimetamp?: UInt64) => Deadline;
export declare const getConfigSetting: (parsedConfig: object, keys: string[], targetTimeUnit?: TimeUnit) => any;
type ConfigObject = {
    [key: string]: string | number | boolean | ConfigObject;
};
export declare const parseConfig: (input: string) => ConfigObject;
export declare const genMetadataCompositeHash: (creatorPublicAcc: PublicAccount, scopedMetadataKey: UInt64, metadataType: MetadataType, targetId: UInt64 | MosaicId | NamespaceId) => string;
export {};
