import { DerivationScheme } from "../../core/crypto";
import { NetworkType, TransactionTypeVersion } from "../model";
/**
 * Static class containing transaction version.
 *
 * Currently transaction version contain:
 * network type (uint8)
 * derivation scheme (uint8)
 * empty byte (uint8) - blank
 * transaction type version (uint8)
 */
export declare class TransactionVersion {
    readonly networkType: NetworkType;
    signatureDScheme: DerivationScheme;
    readonly txnTypeVersion: TransactionTypeVersion;
    /**
     * @param {NetworkType} - Network type
     * @param {DerivationScheme} - Derivation scheme
     * @param {number} - Transaction type version
     */
    constructor(networkType: NetworkType, signatureDScheme: DerivationScheme, txnTypeVersion: TransactionTypeVersion);
    convertToDTO(): number;
    static createInit(networkType: NetworkType, txnTypeVersion: TransactionTypeVersion): TransactionVersion;
    static createFromUint32(version: number): TransactionVersion;
    static createFromHex(hexString: string): TransactionVersion;
    static createFromPayloadHex(hexString: string): TransactionVersion;
}
