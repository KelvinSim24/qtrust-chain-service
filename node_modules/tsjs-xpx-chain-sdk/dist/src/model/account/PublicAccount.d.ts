import { DerivationScheme } from '../../core/crypto';
import { NetworkType } from '../blockchain/NetworkType';
import { Address } from './Address';
/**
 * The public account structure contains account's address and public key.
 */
export declare class PublicAccount {
    /**
     * The account public private.
     */
    readonly publicKey: string;
    /**
     * The account address.
     */
    readonly address: Address;
    /**
     * The account version.
     */
    readonly version: number;
    /**
     * Create a PublicAccount from a public key and network type.
     * @param publicKey Public key
     * @param networkType Network type
     * @param version Account version
     * @returns {PublicAccount}
     */
    static createFromPublicKey(publicKey: string, networkType: NetworkType, version?: number): PublicAccount;
    /**
     * Verify a signature.
     *
     * @param {string} data - The data to verify.
     * @param {string} signature - The signature to verify.
     * @param {DerivationScheme} dSchema The derivation scheme
     * @return {boolean}  - True if the signature is valid, false otherwise.
     */
    verifySignature(data: string, signature: string): boolean;
    /**
     * Verify a signature from hexadecimal string (bytes representation).
     *
     * @param {string} hexString - The data to verify.
     * @param {string} signature - The signature to verify.
     * @param {DerivationScheme} dSchema The derivation scheme
     * @return {boolean}  - True if the signature is valid, false otherwise.
     */
    verifySignatureWithHexString(hexString: string, signature: string): boolean;
    /**
     * Compares public accounts for equality.
     * @param publicAccount
     * @returns {boolean}
     */
    equals(publicAccount: PublicAccount): boolean;
    /**
     * Create DTO object
     */
    toDTO(): {
        publicKey: string;
        address: {
            address: string;
            networkType: NetworkType;
        };
    };
    static getDerivationSchemeFromAccVersion(accVersion: number): DerivationScheme.Ed25519Sha3 | DerivationScheme.Ed25519Sha2;
    static getAccVersionFromDerivationScheme(dScheme: DerivationScheme): 1 | 2;
}
